# File and patterns to ignore during synchronization
# Each line specifies a pattern to ignore
# Lines starting with '#' are comments and will be ignored
# Blank lines are also ignored
# Example patterns:
#
# Ignore all STORED PROCEDURE files with names starting with 'prefix'
# **/PROCEDURE/*/prefix*.sql
#

